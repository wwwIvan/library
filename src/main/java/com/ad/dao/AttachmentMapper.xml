<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ad.dao.AttachmentMapper">
    <select id="findByPid" parameterType="String" resultType="com.ad.model.Attachment">
        SELECT * FROM attachment WHERE pid = #{pid}
    </select>
</mapper>